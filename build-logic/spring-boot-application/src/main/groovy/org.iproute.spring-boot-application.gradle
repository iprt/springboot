plugins {
    // implementation(project(':commons')) // <3>
    id('org.iproute.commons')
    // constraints {api (prefix:)}
    id('org.springframework.boot')
}

dependencies {
    // 能用是因为引入了commons
    // commons 里面又引入了platform
    implementation('org.springframework.boot:spring-boot-starter-web')
}

// 定义了spring-boot-application的构建逻辑

// 引用的时候就是 groovy全类去掉后缀
