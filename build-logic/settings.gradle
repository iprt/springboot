dependencyResolutionManagement {
    repositories {
        gradlePluginPortal()
    }
}

// 构建逻辑 includeBuild('../platforms')
includeBuild('../platforms')

rootProject.name = 'build-logic'
include('commons')
include('spring-boot-application')
include('spring-boot-simple-web')